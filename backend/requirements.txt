# Backend ClipCatcher — requirements pinneados para el MVP del TFG.
# Probado contra Python 3.11 / 3.12 en Windows.

# --- Framework web y servidor ASGI ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.18

# --- ORM y validacion ---
sqlalchemy==2.0.36
pydantic==2.9.2
pydantic-settings==2.6.1
email-validator==2.2.0
python-dotenv==1.0.1

# --- Autenticacion (JWT + hashing de contraseñas) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- Vision por computador (motor del detector) ---
torch==2.5.1
torchvision==0.20.1
opencv-python==4.10.0.84
pillow==11.0.0
