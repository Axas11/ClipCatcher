# Backend ClipCatcher — requirements pinneados para el MVP del TFG.
# Probado contra Python 3.13 en Windows.

# --- Framework web y servidor ASGI ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.18

# --- ORM y validacion ---
sqlalchemy==2.0.36
pydantic==2.9.2
pydantic-settings==2.6.1
email-validator==2.2.0
python-dotenv==1.0.1

# --- Autenticacion (JWT + hashing de contraseñas) ---
# bcrypt pinneado a 4.0.1 porque passlib 1.7.4 no es compatible con bcrypt 4.1+
# (passlib lleva sin actualizarse desde 2020).
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1

# --- OAuth (Google Sign-In, F8) ---
# authlib gestiona el discovery OIDC, intercambio de code y validacion de
# id_token automaticamente; httpx es el backend HTTP que usa internamente.
# itsdangerous lo necesita Starlette SessionMiddleware para firmar la cookie
# de sesion donde authlib guarda el state CSRF entre /login y /callback.
authlib==1.3.2
httpx==0.27.2
itsdangerous==2.2.0

# --- Vision por computador (motor del detector) ---
# torch 2.5.1 no tiene wheels para Python 3.13; bumpeado a 2.7.1 (torchvision
# 0.22.1 es la pareja oficial). Versiones CPU; GPU queda fuera del MVP.
torch==2.7.1
torchvision==0.22.1
opencv-python==4.10.0.84
pillow==11.0.0
